# Development / test environment — full backend test suite (126 tests).
# Includes the production deps plus everything the root tests and the
# mle_professor package need (heavy deps are lazy-imported by that package;
# tests exercise the hash encoder + temp SQLite, but a fresh env should
# still be able to import every code path).
-r requirements.txt

pytest>=7.4
httpx>=0.27

# mle_professor dev/test extras (lazy imports)
numpy>=1.24
pandas>=2.0
pyarrow>=15
lancedb>=0.21
sentence-transformers>=2.2
pypdf>=4.0
arxiv>=2.1
openai>=1.50
