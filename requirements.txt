# Production dependencies — the runtime closure of `uvicorn api:app`.
# The Next.js frontend is a static export served by the API process, so the
# heavy dev/test deps (lancedb, sentence-transformers, ...) stay out of the
# container. See requirements-dev.txt for the full test environment.
fastapi>=0.111
uvicorn>=0.30
groq>=1.0
openai>=1.50
anthropic>=0.30
pydantic>=2.0
requests>=2.32
python-dotenv>=1.0
